import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('WhipUp smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — full integration tests in Phase 2.x
    expect(true, isTrue);
  });
}
